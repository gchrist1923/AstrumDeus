# CMS kotak masuk Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Daftar kotak masuk menampilkan cuplikan 10 kata; klik baris membuka detail utuh (baris baru tetap); buka = dibaca; aksi hanya Hapus.

**Architecture:** Helper murni `ringkasPesan`. List server component memakai overlay tautan. Halaman `[id]` menandai `baru` → `dibaca` lalu merender isi `whitespace-pre-wrap`.

**Tech Stack:** Next.js App Router, Prisma, Vitest.

## Global Constraints

- Bahasa UI Indonesia; ellipsis cuplikan `...`
- Token, radius 0, hit 44px
- Jangan commit kecuali Grace minta
- Worktree: `D:\Me\Work\AstrumDeus-inbox`, cabang `feat/cms-inbox`
- Windows: `$env:Path = "C:\Program Files\nodejs;" + $env:Path`

## File map

- Create: `web/lib/admin/ringkas-pesan.ts`, `web/lib/admin/ringkas-pesan.test.ts`
- Create: `web/app/cms/inbox/[id]/page.tsx`
- Modify: `web/app/cms/inbox/page.tsx`, `web/app/cms/inbox/actions.ts`, `web/app/cms/inbox/inbox.test.ts`

---

### Task 1: ringkasPesan

**Produces:** `ringkasPesan(teks: string, batas?: number): string` default batas 10.

- [ ] Failing tests then implement: kosong `''`; `"satu dua"` utuh; 10 kata tanpa `...`; 11 kata 10 pertama + `...`; newline jadi pemisah.

---

### Task 2: tandaiDibaca + detail page

**Produces:** `tandaiInboxDibaca(id: string, userId: string): Promise<void>` di `actions.ts` — `updateMany` where `id` dan `status: 'baru'`.

- [ ] Source/unit: actions memanggil `status: 'baru'` → `dibaca`.
- [ ] `[id]/page.tsx`: require view, notFound, panggil tandai, `whitespace-pre-wrap`, Hapus jika delete.

---

### Task 3: list

- [ ] Tests: tidak `Dibaca`/`Selesai`/`updateInboxStatus`; ada `ringkasPesan` dan `/cms/inbox/`.
- [ ] Overlay tautan subjek; Hapus `z-10`; cuplikan di kolom Pesan.

---

### Task 4: vitest penuh + browser `/cms/inbox`
